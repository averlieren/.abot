const Queue = require('../queue.js');

module.exports = {
  action: (client, args, message)=>{
    
  },
  'alias': [''],
  'description': 'Play some audio through a voice channel... maybe, only if i wasn\'t lazy'
}
